{
  "name": "grok-traders-web",
  "version": "1.0.0",
  "private": true,
  "description": "GROK TRADERS live desk board",
  "scripts": {
    "start": "python3 server.py",
    "dev": "node scripts/dev.js",
    "build": "node scripts/sync-chain.js && node scripts/build.js",
    "sync": "node scripts/sync-chain.js"
  },
  "engines": {
    "node": ">=18"
  }
}
